#!/bin/bash

#ps -ef : prints a snapshot of every process in the system

#grep mysql : searches for the text mysql from the list of processes on the system

#This is a new a new command:
#searches all the running processes looking for the mysql process

ps -ef | grep mqsql 


